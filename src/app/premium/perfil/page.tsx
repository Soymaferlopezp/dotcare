export const dynamic = "force-dynamic";
export const revalidate = 0;

import PerfilClient from "@/components/dashboard/Profile/PerfilClient";

export default function Page() {
  return <PerfilClient />;
}
